export var GRAVITY_OF_EARTH = 9.81;
export var STEP_SIZES = Object.freeze({
    player: {
        x: 0.25,
        y: 2.25,
    }
});
export var STEP_SPEEDS = Object.freeze({
    player: {
        x: 0.025,
        y: 0.4,
    }
});
